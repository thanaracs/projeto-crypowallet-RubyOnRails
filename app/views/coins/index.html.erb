<p style="color: green"><%= notice %></p>

<h1>Moedas</h1>

<%# <h1><%= Coin.model_name.human(count: @coins.count) %>
<table class="table table-hover">
  <thead>
    <tr>
      <th><%= @coins.human_attribute_name(:descrição) %></th>
      <th><%= @coins.human_attribute_name(:sigla) %></th>
      <th><%= @coins.human_attribute_name(:imagem) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @coins.each do |coin| %>
      <tr>
        <td><%= coin.description %></td>
        <td><%= coin.acronym %></td>
        <td><%= image_tag coin.url_image, size: "25x25" %> </td>
        <td><%= link_to t('links.Mostrar'), coin %></td>
        <td><%= link_to t('links.editar'), edit_coin_path(coin) %></td>
        <td><%= link_to t('links.deletar'), coin, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('new.Add Moeda'), new_coin_path, class:"btn btn-primary" %>
